# 执行返修：B2 核验器与奖励池口径

## 目标

修复 B2 制作拆解交付中独立核验器对当前源码的 3 条 `REFUTE`，并更正与实际奖励逻辑不符的结论。只修 B2 文档和核验脚本，不改游戏代码、资产、覆盖目录或其他 Agent 的交付。

## 当前复核证据

在当前项目版本运行：

```sh
python3 output/external-handoff/B2/verify_b2.py
```

结果为 `共 149 项，CONFIRM 146，REFUTE 3`：

1. `B2-11y` 直接比较 `run.gd` 固定行号 241 / 466。源码已移动，核验器检查到 `extract()` 的 `inventory.clear()`，却拿该行与旧坐标比较；`abandon()` 中也仍有 `inventory.clear()`。改成基于函数边界/语法片段的检查，不使用绝对行号。
2. `B2-13a` 断言 `baseRewardPool` 是实际发放集合的超集，但当前四桌配置中的池条目都能由对应桌的条件奖励分支发放。核验器没有发现多余 ID；B2 README 第 61 行举出的“账房地窖含 ruby-cufflink、余烬桌含 emerald-brooch”与当前 `content.json` 不符。请按当前 `content.json` 与 `run.gd` 逐桌重新核对并改写结论，不要为了让检查变绿而保留错误说法。
3. `B2-13c` 按 `run.gd:432` 固定行号查找 `baseRewardPool.map`，该表达式现位于其他行。改为查找函数/表达式语义，不依赖行号。

可复核的当前源码：

- `Godot/three_d/rules/content.json`：四桌 `baseRewardPool`
- `Godot/three_d/rules/run.gd`：`settle_table()` 的条件奖励分支，以及 `service_view()` 的奖励提示
- `docs/3d-production/external-handoff/B2-production-breakdown/README.md`：第 61 行奖励池结论
- `output/external-handoff/B2/verify_b2.py`：`B2-11y`、`B2-13a`、`B2-13c`

## 执行步骤

1. 逐桌把 `baseRewardPool` 与实际条件奖励分支对照，列出每个 ID 的可达条件；明确“展示候选清单”和“本局实际发放”之间的关系。若当前数据没有超集实例，删除 README 中错误的两个示例，并将结论改成当前源码确实支持的描述。
2. 修改 `verify_b2.py`，令检查跟随函数/数据结构语义，不依赖易漂移的行号；奖励集合检查应从当前配置与发放分支计算，而不是硬编码旧结论。
3. 重新运行 `python3 output/external-handoff/B2/verify_b2.py`，要求退出码为 0、`REFUTE = 0`。同时运行 `python3 output/external-handoff/C1/verify_links.py`。
4. 在 B2 README 增补本轮修订基线、改动项和复验结果；报告检查项数量可以记录为本轮结果，但需注明该数字会随检查脚本变化，不是验收门槛。

## 交付物

- 修订后的 `docs/3d-production/external-handoff/B2-production-breakdown/README.md`
- 修订后的 `output/external-handoff/B2/verify_b2.py`
- 最新 `output/external-handoff/B2/b2-verify.json`
- README 中的复验命令、退出码、`REFUTE` 数及当前奖励池结论

## 边界

- 不修改 `Godot/`、`assets/`、`docs/3d-production/phase-1/coverage/`。
- 不为了满足旧文档结论而改奖励配置或玩法。
- 不扩大为完整 B2 重做；只处理上面 3 条反证及其直接依赖的说明。
- 不 commit / push。
