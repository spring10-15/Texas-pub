# 执行任务 A8：补齐剩余输入与世界交互证据

状态：待用户分派。该任务只补现有目录 ID 的自动化后置证据，不改玩法、不新增 ID、不改存档格式、不生成美术资产。

## 基线与边界

从最新 `origin/main` 开始，先记录 commit、Godot 版本及工作区状态。先读当前树对账和 `current-tree-weak-evidence.csv`，只处理下表锚点。允许修改测试文件和 A8 overlay 生成脚本/交付记录；禁止修改 `transitions.json`、冻结 A8 原始 CSV、规则行为及正式用户存档。

| 源码锚点 | 现有 ID | 要验证的玩家路径与后继 |
|---|---|---|
| `player.gd:43-44` | `world.focus_controls_disabled` | 在输入控制禁用时分别投递移动、鼠标视角和交互输入；位置、朝向、World/Run checkpoint 与交互信号均不变。 |
| `world.gd:511-512` | `world.services_open` | 从玩家视角命中真实吧台服务锚点并按 E；服务面板进入 bar 模式、控制禁用，Run 快照不变。 |
| `world.gd:515-516` | `world.show_run_panel`、`world.extract_confirm` | 从藏匿点实体入口按 E，验证撤离面板打开、费用/行动选择正确；确认撤离前后各自按目录定义断言 UI 与 Run 后继，不把展示面板与实际结算合成一条未解释断言。 |
| `world.gd:518-520` | `world.room_door_blocked` | 从真实账房门锚点按 E，前置未满足时停留原房间、Run 快照不变、阻挡提示出现。 |
| `world.gd:879-885` | `world.services_open`、`world.services_close` | 经真实输入事件触发 B 键：关闭已打开服务面板、再次打开背包/服务面板；分别断言面板状态、输入处理和 Run 快照。 |
| `world.gd:951` | `persistence_restore.playtest_save_blocked` | playtest seed 模式下调用存档，断言返回失败且目标 save 文件原先内容/哈希不变（测试开始前写入隔离临时文件），完整内存 checkpoint 不变。 |
| `world.gd:967` | `persistence_restore.playtest_save_blocked` | playtest seed 模式下预置与当前内存状态不同的隔离存档，调用加载后断言磁盘字节、完整内存 checkpoint、`saving_enabled` 均不变。 |

## 执行要求

1. 优先扩展既有 `player_input_coverage_test.gd`、`spatial_interaction_test.gd`、`playtest_seed_test.gd` 或相邻专用套件；不要为每个 ID 新建单测。
2. 输入绑定证据须走真实 Player/World 信号或 Viewport 输入派发，不得只直接调用 `world.open_services()`、`show_run_panel()` 等被测目标函数来代替输入链。
3. 每个负向/幂等例都比较操作前后的完整 `checkpoint_state()`，并分别断言面板、位置、朝向或信号等可观察后继。
4. 所有临时存档必须放在隔离 `user://` 名称，核验后清理；禁止接触正式用户存档路径。
5. 对账脚本须把完成证据的对应行从 `catalogued_weak` 升为 `catalogued_strong`，记录实际测试与报告路径；其余弱证据保留，不得为了让数字归零而改分类。

## 验收与交付

运行定向测试、全量 `python3 Godot/three_d/tests/run_regression.py --timeout 180`、`python3 Godot/three_d/tests/collect_coverage.py`、`python3 -m unittest discover -s Godot/three_d/tests -p 'test_*.py'`、A8 当前树对账、A8 冻结锚点 `--check`、C1 链接检查与 `git diff --check`。全量回归须全绿，当前 ID 数保持 394，未映射候选保持 0；覆盖率仍写 `unavailable`，不得宣称目录完整或 Phase 1 通过。

成品：仅提交相关测试、A8 当前树 overlay/短结果记录。结果逐行报告输入路径、状态断言、运行命令及退出码，并说明仍留在弱证据表中的条目。
