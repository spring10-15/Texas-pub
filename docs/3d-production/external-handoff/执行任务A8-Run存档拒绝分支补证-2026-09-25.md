# 执行任务 A8：Run 存档拒绝分支补证

> **状态：主 Agent 已完成（2026-09-25），无需重复派发。** `run_restore_bounds_test.gd` 现有 58 个单字段非法样例，覆盖本任务所列拒绝守卫；每例断言恢复返回 `null`、输入快照不变、活体 Run 不变。定向测试通过，全量 Godot 回归 64/64、Python 单测 6/6、A5 核验通过。当前树对账将 8 条 RunCheckpoint 拒绝分支从弱证据升为强证据，弱证据总数由 18 降至 10；没有增加目录 ID。回归报告：`output/3d/regression/20260925-153623/report.json`。

以下内容保留为复现清单；不再作为待执行任务。

基线：从最新 `origin/main` 开始，先记录 commit、Godot 版本、工作区状态。只补自动化证据，不改玩法、存档格式或目录 ID。本任务仅覆盖 `RunCheckpoint.restore()` 中当前 A8 弱证据表列出的 Run 状态拒绝分支。

## 固定范围

源码：`Godot/three_d/rules/run_checkpoint.gd::restore()`。

当前 A8 弱证据锚点与既有语义归属：

| 源码锚点 | 必须构造的非法快照 | 既有结果 ID |
|---|---|---|
| `33-34` | 分别令 `vault`、`cash`、`heat` 为负数；令 `heat=7`；令 `table` 为非 Dictionary | `persistence_run.invalid_fields_rejected` |
| `70-71` | 指定不存在的 `scene_id` | 同上 |
| `90-91` | `offer_index=-1` 与等于/超过当前 fixedRoutes 长度 | 同上 |
| `96` | `arrival_completed > completed.size()` | 同上 |
| `97-101` | `venue_history` 含非字符串、未知场景、重复场景、末项不等于 `scene_id` | 同上 |
| `102` | `transfer_log.size()` 与 `venue_history.size()-1` 不符 | 同上 |
| `103-109` | 转场 hop 的 from/to 顺序错误、fee 非 int 或小于 15、`after_tables` 非 int、非递增、越界或超过 completed 数，以及 `arrival_completed` 与最终 hop 不符 | 同上 |
| `116-117` | 未知抵押物、可用道具冒充贵重物、无活动牌桌却保留抵押物 | 同上 |

## 操作步骤

1. 阅读 `run_restore_bounds_test.gd`、`venue_transfer_test.gd` 和上表源码。以 `RunCheckpoint.capture()` 生成的合法状态作为基线；合法转场历史与抵押牌桌基线必须优先通过公开 `Run.start()`、`transfer_venue()`、`enter_table()` 构造，不手工拼合法快照。
2. 在 `run_restore_bounds_test.gd` 增加表驱动负例。每例只破坏一项字段，调用 `Checkpoint.restore(case, content)` 后断言返回 `null`，并断言原始快照逐字等于调用前副本、原 `Run` 的 `Checkpoint.capture()` 不变。转场历史相关拒绝从一份真实成功转场快照深拷贝后单点篡改。
3. 每个锚点至少覆盖一个实际触发该守卫的样例；`33-34`、`97-101`、`103-109` 的同一源码行多条件要分开构造，避免一个样例意外被更早守卫拦截。失败报告须包含样例名和触发的预期守卫。
4. 不增加 `transitions.json` ID。所有这些拒绝仍归入 `persistence_run.invalid_fields_rejected`；若某个预期样例先命中其他守卫，修正夹具而不是新增 ID。
5. 更新 `reconcile_current_tree.py` 对应锚点的证据引用/强度，并重建当前树 overlay。不得编辑冻结 A8 原始 CSV 或把 defensive restore 输入计入正常玩家路径分母。

## 验收命令

```sh
python3 output/external-handoff/V1/run_godot.py --log output/3d/run-restore-bounds.log --timeout 180 --path Godot -- --script res://three_d/tests/run_restore_bounds_test.gd -- --test
python3 Godot/three_d/tests/run_regression.py
python3 Godot/three_d/tests/collect_coverage.py
python3 -m unittest discover -s Godot/three_d/tests -p 'test_*.py'
python3 docs/3d-production/external-handoff/A8-global-catalog-audit/reconcile_current_tree.py
python3 output/external-handoff/A8/build_a8.py --check
python3 docs/3d-production/external-handoff/A5-persistence-audit/repro/verify_outcomes.py
python3 output/external-handoff/C1/verify_links.py
git diff --check
```

要求定向测试、全回归、Python 单测、A8/A5/C1 核验均退出码 0；覆盖目录 ID 数不变；当前树弱证据中的上述 RunCheckpoint 锚点不得再标 weak。报告需列出逐锚点样例名、拒绝后快照不变断言、尚未覆盖的分支和所有命令结果。

## 成品边界

只提交 `run_restore_bounds_test.gd`、A8 当前树对账脚本/生成文件及简短结果说明；不改规则、内容、存档格式、正式用户存档或 Blender/Godot 美术资产。不得宣称全局状态转移分母完整、覆盖达到 95% 或 Phase 1 通过。
