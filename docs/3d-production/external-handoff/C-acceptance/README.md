# 任务 C：阶段验收与试玩材料（交付说明 · C1 返修后）

初版执行日期：2026-09-21（基线 `b158491`）。**C1 返修：2026-09-22（基线 `413dce9`）。C2 返修：2026-09-22（基准 `c54683b` / `8382b7b`；R1–R4 返修基线 `bcc614a`）。**

> 本文件已按主 Agent《外部交付验收与后续任务（2026-09-21）》第 5 节 C1 返修，并按《外部交付复验与任务续接（2026-09-22）》C2 五条与《B2 / C2 复验与返修（2026-09-22）》C2 必修项 R1–R4 返修。逐条记录见同目录 `corrections.md` 与 `C2-corrections.md`。
> **返修后已做对抗性审查**：独立重推导检查**全部复现一致、0 条被反例推翻**（条数会随脚本增删漂移，故不写死），另修掉 3 处表述缺陷（含 1 条自相矛盾的复现命令、2 处行号不精确、DC-04 撤回理由过强）。见 `docs/3d-production/external-handoff/对抗性审查-B1C1-2026-09-22.md`；复现脚本 `output/external-handoff/adversarial/adversarial_audit.py`（判定只认 `REFUTE = 0` / `WEAK = 0`）。
>
> ⚠️ **这句只是那次静态检查的结果，不等于整份交付通过**：它不含运行时验证（本轮没打开游戏）、不替代 `production-gates.md` 的 Phase 1 五条门槛、也不能用来宣布 Phase 1 通过。主 Agent 复验已就此加了限定，原文见对抗性审查文档 §一 的限定框。

> **当前状态补记（2026-09-25）**：本文后续表格保留 C 交付时的历史快照。当前门槛以 `docs/3d-production/phase-1/production-gates.md` 为准：X=50；决策事件绝对差口径已定但真人记录缺失；全局状态目录现登记 389 项，最新同树回归后 389/389 项均有当前证据，仍为 `incomplete_catalog` 且全局覆盖率为 `null`。扑克、世界、存档既有审查分组的待办数组为空，但源码驱动的全局分母复核仍发现 19 条玩家可达候选及 19 条弱证据项待核，不应把这些分组数组读成全局闭合。128 个酒馆×牌桌×对手组合现已有控制策略和生产 AI 两套牌桌流程（256 组合）；河道老鲨参数微调后的最新矩阵为 20,119 项检查通过。该样本不证明完整一晚体验、胜率平衡或真人可识别性。DC-03 的回归命令已于 2026-09-24 修正，见 `doc-corrections.md`。

**这一轮做了什么、没做什么，先说清楚：**

- 做了：把 `production-gates.md` 的五条 Phase 1 门槛逐条翻成可执行清单；核对每份 Phase 1 产出并标"已证明/部分证明/缺失/过时"；把 `first-run-walkthrough.md` 重写成 17 步可执行清单；准备两份真人记录模板；**提出 7 条**文档冲突问题并给出替换文本；排出完成 Phase 1 尚需的动作。
- **没做**：没有打开游戏，没有真人试玩，没有计时，没有性能测试。
- 因此本目录里**没有任何一行写着"试玩通过"**。未执行的体验检查一律标注为"文档核查"或"未执行"。
- **"本轮没有执行"只描述本轮范围，不等于"从来没有发生过"。** 见 `gate-matrix.md` 通用限制第 5 条。

---

## 一、交付文件

| 文件 | 内容 | 谁用 |
|---|---|---|
| `gate-matrix.md` | 6 条完成门槛 + 6 项必交产物 + 跨酒馆 3 项 + 9 条"明确未验证"清单 + 8 个待办动作 + **资金守恒闭合边界**（§一之二） | 主 Agent / 用户 |
| `walkthrough-checklist.md` | 17 步可执行检查清单（含前置条件、玩家动作、预期反馈、失败判断）+ 3 条附注 | 试玩组织者 |
| `decision-record-template.csv` | **20 列**决策记录模板（`机会ID`/`机会计数`/`关联尝试` 三列可复算机会数；含 2 组**虚构示例**共 22 行，正式收集前请删掉示例行） | 试玩组织者 / 分析者 |
| `first-play-template.md` | 首次游玩记录模板（时长与 `T_extract`、`N` 与机会总数、卡住位置、资金结算与金库链条、控制理解、截图编号） | 试玩组织者 |
| `real-player-protocol.md` | **一页待执行的真人协议**：参与者分类、相同种子/路径、决策差异计数（配对键 = `机会ID`）、分叉后不配对、撤离费用通式与四家参数、记录表填写示例（示例与真人数据分开） | 试玩组织者 |
| `decision-metric-example.md` | **C2 修订**：一组**明确标注为虚构**的**合格**统计示例（与 CSV 示例行同源，分叉点 = 第一处不同的决定），另附原跨酒馆版本的**不合格对照（作废）**与计入分类规则——只证明表格能算，**不是玩家数据** | 试玩组织者 / 分析者 |
| `doc-corrections.md` | **7 条**文档冲突（原位置 + 代码证据 + 建议替换文本）+ 8 项已核对一致的部分 | 主 Agent |
| `corrections.md` | C1 返修记录（逐条「修订前 → 新证据 → 修订后」） | 主 Agent |
| `C2-corrections.md` | **C2 新增**：按主 Agent 五条限定修订协议/CSV/示例的记录 + `world.gd` 行号漂移映射表 + 仍待落实步骤 | 主 Agent |
| `README.md` | 本文件：验收缺口与优先顺序 | 主 Agent |

### 文档冲突条数（统一口径）

| 项 | 数 |
|---|---:|
| 累计提出 | **7**（DC-01 … DC-07） |
| 主 Agent 已处理 | 2（DC-01、DC-02，C 交付时） |
| 已撤回（经复核不成立） | 1（DC-04） |
| 仍待处理 | **1（DC-03，C 交付时；已于 2026-09-24 处理）** |
| 仅需范围/日期补注（非错误） | 3（DC-05、DC-06、DC-07） |

> 初版本节与 §二 写"6 处"，与 `doc-corrections.md` 的 7 条不符；DC-04 撤回后**有效条数为 6**。返修后统一按上表表述，两处口径一致。

---

## 二、验收缺口（按"卡住 Phase 1 的程度"排序）

| 优先级 | 缺口 | 为什么排这个位置 | 谁能解 |
|---|---|---|---|
| **P0-A** | **全局状态转移分母未枚举**（当前 `status=incomplete_catalog`，`overall_state_transition_coverage=null`，目录 357 项均有当前证据；未拆完的族：world / persistence） | 门槛 4 要求 ≥95%，但**分母不存在**，因此仍不能计算通过率 | 主 Agent（继续逐族枚举并为新结果补后继状态测试） |
| **P0-B · 已解决** | X 已定义为 50；四酒馆各三组连续 100 个种子窗口的组合数最低 72 | 该自动统计证明组合多样性门槛，不证明真人感知到差异 | 口径与结果见 `docs/3d-production/phase-1/seeded-pools.md` |
| **P1-A** | **"新老手决策差异 >3"口径已定，但本轮仍无正式记录** | 门槛 5。主 Agent 已在复验中定死口径：**主指标 = 实际决策事件数的绝对差 `abs(N_new − N_experienced) > 3`**，选项分歧只作辅指标（且按定义最多为 1、不能当门槛）；配对键是 `机会ID`、**分叉之后不配对**；计数单位、排除项与"未作选择 ≠ 执行拒绝"的分类见 `real-player-protocol.md` §3 | 用户组织试玩（固定种子入口已就绪） |
| **P1-B** | **本轮无真人验收记录** | 门槛 6（20–30 分钟）和 Phase 4（5 位外部玩家）都依赖它。这是**唯一无法用代码推进**的部分 | 用户组织 |
| **P2-A · 已完成** | 四酒馆×四桌×八对手分别跑控制策略与生产 AI 两套牌桌流程，共 256 组合；最新参数下 20,119 项检查通过 | 前桌解锁由夹具提供；生产 AI 对手能弃牌/全押，但玩家策略固定，不代表胜率平衡或真人可识别性 | 归档证据见 `docs/3d-production/phase-1/opponent-profiles.md` 与 `output/3d/roster-showdown.json` |
| **P2-B** | 资金守恒缺"多贵重物 × 预约有效/过期 × 四店"矩阵 | 门槛 1 的补完；现有 33,768 组合固定了背包，覆盖面有洞。**口径已按 §一之二 闭合边界修正** | 主 Agent |
| **P3** | DC-03 已处理：`checkpoint-progress.md` 补了两条可复制命令并限定 `-- --test` 的适用范围；DC-05/06/07 的历史范围补注按各条状态继续核对 | 纯规则测试带 `-- --test` 无害但非必需；实例化 `main.tscn` 的测试必须隔离正式存档 | 主 Agent |
| **P3** | `patternPunish` 强度、`signal_analysis` 阈值口径、`payout` 覆盖面未证明 | 属细节正确性；已在 `gate-matrix.md` §四 列为"明确未验证" | 主 Agent |

---

## 三、三个必须分开、不能互相替代的验收通道

| 通道 | 现状 | 本轮证据 |
|---|---|---|
| **自动规则测试** | 有。`b158491` 时 45 个 `*.gd`（任务 A 实际执行 46 次）；`413dce9` 为 51 个；`8382b7b` 快照为 53 个、19 个实例化 `main.tscn` 的测试（需 `-- --test`）；**2026-09-25 代码/回归基线 `886d1a7`**，测试目录有 60 个 `.gd` 文件、其中 22 个引用 `main.tscn`，最近一次完整回归为 **57/57 通过**；覆盖目录 343/343 项有当前证据，但仍为 `incomplete_catalog`，全局覆盖率不可计算。 | A 任务旧基线运行记录见 `../A-regression/`；当前回归报告见 `../../../../output/3d/regression/20260924-235534/report.json` |
| **真人体验测试** | **本轮 0 条正式记录** | 本目录只交协议与模板，未执行 |
| **性能测试** | **0** | 属 Phase 3（两平台中端机 ≥60 fps、≥30 分钟无掉帧/泄漏），本轮未测 |

> 初版写"29 个 GDScript + 2 个 Python 汇总器"，与 `gate-matrix.md` §二 的"45 个 `*.gd`"不一致。两个数字都不是当前值，返修后统一为**上表单一口径（带基线日期）**。

**不得互相替代。** 具体地：
- 自动测试全绿**不代表**真人能上手（gate 2/3/5/6 全部依赖真人）。
- 真人玩得顺**不代表**账目守恒（gate 1 需要断言）。
- 帧率截图**不代表**状态转移覆盖率（`production-gates.md` 原文已明确）。

---

## 四、时间口径（最容易混的两个）

| 指标 | 目标 | 归属 | 本轮状态 |
|---|---|---|---|
| 正常局时长 | 20–30 分钟 | Phase 1 门槛 6 | 未测 |
| 首局教程时长 | <5 分钟 | **Phase 4**，不是 Phase 1 | 不存在（Phase 4 未开始） |

两个数字必须分开记录（`first-play-template.md` §1 与 `walkthrough-checklist.md` 附注 1 已分别留位）。

---

## 五、边界声明

- 未修改游戏规则、世界场景、Blender 源文件、GLB、贴图、角色动画或既有统计目录。
- 未覆盖 `first-run-walkthrough.md`、`difficulty-curve.md`、`seeded-pools.md`、`cross-venue-evening.md`、`checkpoint-progress.md` 等原文；冲突一律以"原位置 + 建议替换文本"形式给出（`doc-corrections.md`）。
- 未启动游戏编辑器，未生成图片/视频/音频，未购买素材。
- 未启动其他子 Agent，未 commit / push / 部署。
- 未读写玩家真实存档。
- 本文件**不代表**外部五位玩家验收已发生；`production-gates.md` 的"进行中；没有任何整体通过声明"维持不变。
- C1 返修只动了本目录内的文档（`README.md` / `gate-matrix.md` / `walkthrough-checklist.md` / `doc-corrections.md`），新增 `corrections.md` 与 `real-player-protocol.md`；未改任何被引用的项目文档，未改代码。
- C2 返修只动了本目录内的文档与 CSV（`real-player-protocol.md` / `decision-record-template.csv` / `first-play-template.md` / `decision-metric-example.md` / `C2-corrections.md` / 本文件）；未改游戏代码、资产或覆盖目录，未 commit / push。

## 六、返修记录、路径约定与链接自检

- 逐条「修订前 → 新证据 → 修订后」见 `corrections.md`（C1）与 `C2-corrections.md`（C2，含 C2-1…C2-9）。

**可复算脚本（C2 起）**

| 脚本 | 作用 |
|---|---|
| `output/external-handoff/C2/build_records.py` | 生成 `decision-record-template.csv`（表头 + 两组虚构示例行） |
| `output/external-handoff/C2/verify_c2.py` | 从 CSV **逐行重算** `N` / 机会总数 / 主指标 / 分叉时点 / 可比局面 / 分歧数 / 手续费 / 到账 / 金库落点，并核对代码事实；结果写 `output/external-handoff/C2/verify_c2_result.json` |

**路径约定（本目录统一口径）**

| 引用形式 | 解析基准 |
|---|---|
| Markdown 链接 `[文本](目标)` | **相对文档所在目录**（真正的相对链接，必须可直接打开） |
| 反引号里的文件路径（如 `` `Godot/three_d/rules/run.gd` ``） | **相对项目根目录**（`~/Desktop/Claude/项目集群/Gen 项目集群/1、德扑酒馆：落袋为安/`） |

> 为什么写清楚这条：本轮自检发现初版有两类断链——① 把若干**相对 `docs/3d-production/` 的简写**（表现为 phase-1/xxx.md、coverage/README.md 这种形式）当成通用路径，从 C 目录打不开，已统一改写为相对项目根的完整路径；② 把命令串 `` `python3 output/.../b1_audit.py` `` 当成路径。两类都已修。（上文的"误写形式"刻意不加反引号，避免被自检脚本计成断链。）

**自检结果（脚本 `output/external-handoff/C1/verify_links.py`，可重跑）**

| 项 | 结果 |
|---|---|
| Markdown 链接 | **0 条不可解析**（交付文档里本就没有真正的相对 Markdown 链接：原有 1 条引用原文的链接已改为纯文本并注明原目标） |
| 反引号路径 | **0 条两级解析都失败**（其余按上述约定可解析：少数文档相对、其余仓库根相对。**条数随文档编辑变化，不是固定指标**，以重跑输出为准） |
| 两级解析都失败 | **0 条** ✅ |

> 自检脚本会跳过两类**非引用**文本，避免误报：① 写在反引号里的"链接写法示例"（如约定表里的格式示范）；② 含空格的命令行（如 `` python3 output/external-handoff/B1/b1_audit.py ``）。
