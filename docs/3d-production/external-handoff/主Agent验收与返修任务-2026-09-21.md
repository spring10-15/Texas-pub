# 外部交付验收与后续任务

日期：2026-09-21。验收基线：b158491，加主 Agent 本轮测试与计划修订。由用户把对应章节转交外部 Agent；不在当前账号启动子 Agent。

## 1. 总体结论

五阶段目标保持不变，当前仍为 Phase 1。基础模型存在不等于高保真验收通过；先收口规则、覆盖分母和真人验证口径，再进入内容与精模阶段。Blender、核心规则和最终集成仍由主 Agent 负责。

| 交付 | 结论 | 处理 |
|---|---|---|
| A：回归 | 接收本轮执行证据；不等于游戏整体通过 | 可开始 A2；补充统计单位说明 |
| B：内容与资产 | 暂不通过事实准确性验收 | 先返修 B1，不能据此安排重建资产 |
| C：验收与试玩 | 模板结构可用，验收口径暂不通过 | 先返修 C1，避免误报覆盖缺口与计时结果 |

A 的结果表有 48 行，44 PASS、2 FAIL、2 非测试，所列日志路径均存在。主 Agent 没有把这些历史 PASS 冒充全部独立复跑。B 内容表 56 行、资产表 14 行，CSV 可以解析；这只能证明格式完整，不能证明结论准确。C 没有实际执行真人试玩，模板不能作为体验通过证据。

## 2. 主 Agent 本轮已处理

- `spatial_interaction_test.gd`：取消“货架必有 marked-lens”的假设，从实际货架取商品和交互锚点；站位跟随商品，保留真实物理射线与购买入包断言。初次调整站位时发现碰撞会把玩家推离目标，已调整为货架前方的站位，未绕过遮挡检查。
- `two_tables.gd`：第二桌左侧名字改为核对实际牌局左席角色，不再写死河道老鲨。
- 修复后复验：空间交互连续三次各 41 项通过，双桌流程 27 项通过。日志在 `output/3d/handoff-review/`。这两项修复针对测试，不宣称修好了未复现的游戏缺陷。覆盖汇总再次验证 239 个已登记结果证据有效，全局分母仍未完成。
- 更新 `phase-1/production-gates.md` 的执行顺序；修正难度曲线、种子池文档中“跨酒馆未实现”的过时描述。

## 3. A2：边界问题复现与回归补录（现在可以开始）

### 目标

把 A 报告末尾的三条线索变成可判断的证据，避免主 Agent 根据猜测改规则。只做诊断，不修改正式规则或正式测试。

### 步骤

1. 记录当前 HEAD、Godot 版本和工作区状态。复跑本轮修正的空间交互、双桌测试，命令使用 `-- --test`。保留原始日志；截图工具不计入测试套件数。
2. 庄位轮换：分别让座位 1、座位 2 出局，记录连续至少三手的庄家、小盲、大盲、首行动者。通过 `next_hand` 推进，明确区分用于设定初始筹码的夹具与实际行动路径。预期规则来源和实际结果分列；不要只按手数取模推导“正确结果”。
3. 首次进攻优惠：记录优惠前后实际扣筹码、目标下注额、底池、其他玩家跟注额、全桌总财富。覆盖玩家/对手使用优惠、余额刚好与不足、下一手重置。把设计语义疑问与守恒错误分开，不自行把优惠删除。
4. 存档恢复：仅使用测试专用目录/文件，检查截断文件、缺字段、非法状态、旧版本及 RNG 恢复；对照恢复前后完整状态。不能安全隔离时标记未执行，不能碰玩家存档。
5. 每条线索写最短复现步骤、输入、预期、实际、稳定性、日志与脚本路径。复现失败也交证据，不强行判缺陷。

### 成品

放入 `docs/3d-production/external-handoff/A2-boundaries/`：`README.md`、`findings.md`、`test-results.csv`；可运行诊断脚本放同目录 `repro/`，日志放 `output/external-handoff/A2/`。只写自己的目录，不修改生产代码、不生成模型、不提交推送。

### 验收

三条线索逐条有结论及可重跑证据；日志扫描脚本错误，不能只看退出码。断言数、语义结果数、脚本数分别统计（例如 ending 的 7 个结果不等于内部断言数）。需要主 Agent 决定的优惠语义单列，不代替决定。

## 4. B1：资产与内容事实返修（先完成，不新增制作任务）

以下改动需同步 README、content-matrix.csv、asset-gaps.csv，保留“修订前结论 → 新证据 → 修订后结论”的记录。

| 必须修正 | 当前证据与正确边界 |
|---|---|
| 酒保被判为 GLB 未接入 | `scripts/world.gd` 调用 `characters.build`；`scripts/characters.gd:6-30` 加载 bartender.glb，并隐藏 Bartender 占位盒体。不能因 bar_display 仍创建盒体就判模型未接入。继续追踪 deliver 的动画入口，区分遗留盒体与可见人物。 |
| 以三种顶点数断言只有三套网格 | 顶点数相同不证明几何相同。核对 build_characters.py 的人物参数、几何差异和九个导出文件；未比较几何就标记“独特性未验证”，不能写成已证实只有三个人物。 |
| 九件道具被称为九个独立 GLB 文件 | 实际为 interactive-props.glb 内的命名部件；props-export.json 包含 12 个部件。区分文件、网格、道具三种数量。 |
| 十件贵重物被一概判为无源文件、无 GLB、3D 不可见 | 逐件检索 stash 建模脚本与导出物，区分静态装饰、独立可复用模型、背包/奖励交互接入。装饰存在不等于可用道具已完成；独立道具缺失也不等于任何场景都没有相似实体。 |
| 四件贵重物占两格 | content.json 当前仅 sealed-bond、obsidian-idol、vault-promissory 三件为 slots=2，其余贵重物为 1。 |
| “部分实现”和“未单独测试”混用 | 分成实现状态、验证状态两列；没有单独售卖测试不能直接写成没有售卖逻辑。 |
| 字段读取结论过强 | baseRewardPool 在 run.gd 的情报文本中有读取；应写“用于展示，奖励发放是否同源另核对”，不能说零读取。其他字段也区分展示、结算、旧存档兼容入口。 |
| 计数及历史兼容 | 56 行不要再写成 55；opponentIds 的 seed=0/旧存档回退不能当作可随意删除的无效字段；账房地窖也不是第四个牌室。 |

### 操作与成品

1. 按“内容 ID → 生成脚本 → 导出文件 → 加载函数 → 运行使用点”逐项追踪。
2. 源文件读取与 GLB 元数据检查即可；不执行 build/export，不覆盖资产，不生图。
3. 修订原 B 目录三份文件，新增 `corrections.md`；对 19 物品和 9 人物提供逐实体追踪行，可复用现有矩阵，不另建资产数据库。
4. 验收：关键断言都有代码/文件证据，未验证项明确标记；不能把接入缺口升级成“全部重新建模”的任务。

## 5. C1：验收口径与试玩清单返修

1. 撤回 doc-corrections 的 DC-04：coverage/README 在“胜者与边池独立金额预期”中已有 payout 六种结果，故不能补回“胜者分配完全未登记”的待办。gate-matrix 也要删除声称 pending_families 仍包含 showdown_winner_allocation 的错误描述。保留其尚未穷尽所有牌型/下注路径的真实局限。
2. DC-07 改为“历史快照阅读提示”：226、232、239 是实现批次演进，不是同时成立的当前计数冲突。保留历史，当前状态指向最新汇总。
3. DC-01/02 已由主 Agent 修正文档，返修记录标明已处理。DC-05/06 使用范围/日期补注，不把旧版设计与历史协作边界说成当时写错。
4. `-- --test` 应作为世界交互测试的标准命令，避免加载/自动保存真实存档及失焦暂停干扰；不得宣称所有纯规则测试缺此参数都会失败。
5. walkthrough 的整局起点改为“确认出发”，首桌开始另记；撤离完成为结束。单桌安全路线与 20–30 分钟正常局分开，不能用单桌耗时直接判正常局门槛。
6. 核对第 3 步本金：实际 `min(300, 金库)`，并非金库低于 1200 就带不满 300。计费公式写清取整、条件和其他修正，建议以实际报价为核对主据。
7. gate-matrix 中资金守恒要有闭合边界：玩家金库与现金之外，还存在桌上筹码、对手转移、物品价值、奖励和费用；不能无条件用“金库变化 + 随身变化 + 损失 = 0”判断所有路径。
8. 真人“没有任何数据”改为“本轮没有正式记录可供验收”，不否认用户此前已试玩。分组人数、5 局中位数等是建议协议，不能悄悄升级成已确认门槛。统一 README/矩阵中的冲突条数。

### 成品与验收

修订 C 目录原文件，新增 `corrections.md`。交付一页待执行的真人协议：参与者分类、相同种子/路径、决策差异计数、正常局起止时间、记录表填写示例；示例与真人数据明确分开。没有真实执行就保持未执行。所有相对文件链接须验证能从文档所在目录打开。

完成 B1/C1 后交回主 Agent 复验，再领取下一批任务；不要自行展开精模、规则修改或更多 Agent 分工。
