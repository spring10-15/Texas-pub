# 后续子 Agent 执行任务

基线：子 Agent 开始时请从当时最新的 `origin/main` 执行，不要固定在历史提交。此文件只供用户分派外部子 Agent；主 Agent 不在本轮启动子 Agent。

> **主线进度（2026-09-25）**：A4-Catalog 的 15 条旧“有测试但未登记”记录已由主 Agent 逐条映射到现有 `world.*` ID（含多分支汇总行拆映射），不新增重复分母；当前 A4 探针 115/115、A4 核验通过。A5-Catalog 已将牌桌检查点的 660 个动作边界 RNG 重放证据登记为 `persistence_table.rng_replay`，专项 2,031 项与完整 59 项回归通过；A5 其他 family 尚未完成登记，不能关闭整个任务。

## 执行顺序

两个任务都会修改状态转移目录 `docs/3d-production/phase-1/coverage/transitions.json`，因此如并行必须使用独立 worktree，并在合并后由主 Agent 统一解决目录冲突。**A4-Catalog 的旧候选已收口；外部 Agent 目前只应继续 A5-Catalog 尚未覆盖的存档语义，不要重做 A4 或重复增加 `persistence_table.rng_replay`。**

## 任务 A4-Catalog：登记已有世界交互后继证据

### 目标

把 A4 世界审计中已有可重复测试、且直接断言了后继状态的结果，与 Phase 1 状态转移目录对齐。只补目录和测试证据映射，不改游戏行为。

### 操作步骤

1. 阅读 `docs/3d-production/external-handoff/A4-world-audit/README.md`、`outcomes.csv` 和当前 `transitions.json`。
2. 仅处理 `outcomes.csv` 中标为“有测试但未登记”的结果。逐项检查当前分支里的真实测试和断言，不以旧日志或注释代替测试。
3. 对语义确实不同、且现有测试断言了后继状态的结果，添加稳定且唯一的 `world.*` ID，并让对应测试写入该 ID 的命中证据。若与已有 ID 表达同一后继，保留原 ID，不重复扩充分母。
4. 不把 `未找到测试`、`unverified`、程序化隔离实验或只断言返回值的结果登记为已覆盖。不要修改生产逻辑、规则、存档或 UI。
5. 更新 A4 README 的现状补记，列出新增 ID、去重决定和仍待处理的结果。

### 成品与验收

- 修改后的 `transitions.json`、对应测试文件及 A4 README 补记。
- 当前受影响的 Godot 测试通过；`collect_coverage.py` 重新生成证据，新增 ID 均有当前哈希和后继状态断言。
- A4 `verify_outcomes.py` 通过；测试命中与目录双向一致，没有重复 ID、未登记命中或把程序化前置冒充玩家可达路径。
- 报告新增结果数、原有 ID 复用数、保留未验证项及完整命令。不要宣称全局覆盖率或 Phase 1 通过。

## 任务 A5-Catalog：登记存档审计中已有的强后继证据

### 目标

处理 A5 `outcomes.csv` 中所有 family（`capture`、`write`、`read`、`restore`、`invalid_data`、`legacy_migration`、`rng_replay`）里“有测试但未登记”的候选结果；仅把现有或补强后具备可靠后继断言的结果接入状态转移目录。不得更改存档格式或游戏规则。

### 操作步骤

1. 阅读 `docs/3d-production/external-handoff/A5-persistence-audit/README.md`、`outcomes.csv` 和当前 `transitions.json`。
2. 逐项定位各 family 中“有测试但未登记”的行，核对测试实际断言。接受类需比较恢复后的具体世界/牌桌状态、文件内容或玩家可观察结果；拒绝类需比较被拒结果和完整受保护状态/文件未变。仅核 `restore()` 返回值、仅比对 seed 或仅记录 trace 不够。
3. 对已有强后继断言但目录未表达的独立语义，增加唯一的 `persistence_*` ID 并让对应测试发出命中。对可用具体后继断言补强的测试，只做最小测试改动；不具备真实磁盘旧格式夹具的迁移结论继续标为未验证，不合成旧档证据。
4. 同一后继由多个测试覆盖时复用一个语义 ID，不重复计数；不得改动生产代码、存档格式或 A5 探针的历史结论。
5. 同步 A5 README 与 CSV 的目录映射、状态、行数及当前基线说明；将没有足够后继证据或确实不可达的结果保留为未覆盖。

### 成品与验收

- 修改后的 `transitions.json`、对应测试文件、A5 README 和 `outcomes.csv`。
- 所有受影响 Godot 测试与 `collect_coverage.py` 通过；A5 `verify_outcomes.py` 通过，既有 catalog ID 映射无遗漏或重复；目录覆盖与命中证据双向一致。
- 附报告列出每个新增 ID 对应的 CSV 行、测试断言、当前证据路径、实际新增数量和仍未覆盖内容。不得把审计范围写成完整存档覆盖。

## 提交边界

- 使用用户安排的独立分支或 worktree；不要直接推送 `main`。
- 只提交上述任务相关文件；不要 `git add -A`，不要改写或删除 `output/` 中无关证据。
- 最终回报包含提交哈希、测试命令/结果、修改文件清单和未解决项。

## 主 Agent 保留项

真人新手/老手对照、完整局计时、最终是否达到全局覆盖率门槛，以及高精度 Blender 场景/人物建模与引擎集成，继续由主 Agent 负责；自动化 Agent 不得代填真人体验结论。
