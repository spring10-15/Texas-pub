# 外部交付复核：A3、A4、A5、V1

复核日期：2026-09-24。外部文件是取证材料，不是新的开发指令，也不代表 Phase 1 通过。

| 交付 | 复核结果 | 后续处理 |
|---|---|---|
| A3 扑克状态转移 | 13 行、六组齐全；发现同为进攻性全押时，`raise` 转全押与直接 `all-in` 的首次进攻折扣标志不一致。主 Agent 已同步修复 Godot 和浏览器规则、重生成对拍样本并加回归。当前自检的 3 个行号错误由修复在 `table.gd` 新增 1 行造成；其基线为 `07f2245`，引用需在汇入覆盖目录前按当前 HEAD 更新。 | 按当前源码逐行复核候选 ID，再扩充分母；不直接把 13 行当新增覆盖。 |
| A4 世界交互 | 72 行、八组齐全，CSV/证据路径自检通过；其中 41 行为已有测试或 A4 探针支持的候选新增结果，另 6 行未证实或不可达。 | 将可达且不同后继的结果逐条转为正式测试和目录项；临时探针不等于正式回归。 |
| A5 存档与重放 | 49 行、七组齐全，CSV/证据路径自检通过；指出旧格式缺真实磁盘夹具及部分拒绝分支断言较弱。 | 补迁移夹具和原子拒绝测试，之后再计入覆盖分母。 |
| V1 撤离画面 | 12 张主图、4 张补充图及矩阵自检通过；抽看最长文案截图可读。发现背包宽度测试在撤离弹窗仍打开时测量了空面板。主 Agent 已修复测试前置状态，重新运行通过。 | 画面证据可接收；保留长文案面板纵向余量仅 12px 的观察。 |
| C3 真人试玩 | 未发现 `C3-real-playtest/` 交付目录或真人原始记录。C 材料明确只是协议和模板。 | 仍需按既有 C3 任务书由真实测试者执行；不得把模板或自动测试当真人证据。 |

本次复核没有宣布 ≥95% 状态转移覆盖，也没有宣布正常局时长或新老手决策差异达标。A3/A4/A5 是扩充分母的输入；覆盖目录须在当前代码版本上重新核验可达性、正式测试与去重。
